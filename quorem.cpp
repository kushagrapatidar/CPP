//Compute quotient and remainder
#include<iostream>
using namespace std;

int main(void)
{
    int a,b;
    cout<<"Enter an number: ";
    cin>>a;
    cout<<"Enter another number: ";
    cin>>b;
    cout<<"\nThe quotient and remainder of "<<a<<" & "<<b<<" are: "<<a/b<<" & "<<a%b;
}